name: saferoute_backend
description: PostgreSQL-backed API for SafeRoute with authentication and route tracking.
publish_to: none

environment:
  sdk: ^3.10.4

dependencies:
  bcrypt: ^1.1.3
  postgres: ^3.5.11
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  uuid: ^4.5.1
